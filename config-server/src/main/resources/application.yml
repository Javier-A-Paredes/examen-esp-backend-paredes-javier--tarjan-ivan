server:
  port: ${PORT:8888}
spring:
  application:
    name: config-server
  cloud:
    config:
      server:
        git:
          uri: https://github.com/IvanTarjan/config-data-parcial-espBack
          force-pull: true
